{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <a href="/">Home</a>
                <p>This came from the backend: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}